import 'package:flutter/material.dart';

class MatchSetupScreen extends StatelessWidget {
  const MatchSetupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('試合設定画面（開発中）'),
    );
  }
}
